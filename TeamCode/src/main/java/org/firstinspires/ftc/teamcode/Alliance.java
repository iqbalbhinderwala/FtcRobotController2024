package org.firstinspires.ftc.teamcode;

public class Alliance {
    // Team Color
    public enum Color {
        BLUE,
        RED,
    }

    // Starting side
    public enum Side {
        LEFT,
        RIGHT,
    }
}
